 r←Test_OpenAPIResponseNS dummy;router;spec;pathNS;op;respNS
 router←⎕NEW #.Stark
 router.Handlers←#.Mocks.MockHandlers
 respNS←(
    ⍙201: (
        description: 'Created'
    )
 )
 '/items' router.Post ('CreateItem' (responses: respNS))
 spec←router.OpenAPI
 pathNS←spec.paths⍎⊃spec.paths.⎕NL ¯9
 op←pathNS⍎⊃pathNS.⎕NL ¯9
 r←'Created'≡(op.responses.⍙201).description
