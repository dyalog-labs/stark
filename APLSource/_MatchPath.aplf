(matched pathParams)←_MatchPath(pattern path);patSegs;pathSegs;i;seg;name;val
pathParams←⎕NS ''
matched←0
patSegs←(pattern≠'/')⊆pattern
pathSegs←(path≠'/')⊆path
:If (⍴patSegs)≠⍴pathSegs ⋄ :Return ⋄ :EndIf
:For i :In ⍳⍴patSegs
seg←⊃patSegs[i]
:If '{'=⊃seg                           ⍝ named parameter
name←1↓¯1↓seg                      ⍝ strip { }
val←⊃pathSegs[i]
pathParams ⎕VSET ⊂name val         ⍝ pathParams.name ← 'val'
:ElseIf seg≢⊃pathSegs[i]               ⍝ literal mismatch → no match
:Return
:EndIf
:EndFor
matched←1
